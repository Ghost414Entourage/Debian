sudo apt update
sudo apt install podman

podman --version



cat /etc/containers/registries.conf


nano /etc/containers/registries.conf

[registries.search]
registries = ['docker.io', 'quay.io', 'registry.fedoraproject.org']


Edit the /etc/containers/storage.conf file to customize storage options, such as the driver or location of container images.


Podman uses CNI (Container Network Interface) plugins for networking. Ensure the necessary plugins are installed: 

sudo apt install containernetworking-plugins 

Adjust system limits to allow non-root users to run more containers: 

echo "user.max_user_namespaces=28633" | sudo tee -a /etc/sysctl.d/userns.conf
sudo sysctl -p /etc/sysctl.d/userns.conf



sudo podman completion bash | sudo tee /etc/bash_completion.d/podman

If you're transitioning from Docker, you can create aliases to use Podman commands with the docker prefix:

echo "alias docker=podman" >> ~/.bashrc
source ~/.bashrc






# Create a network
podman network create my-network


# List networks
podman network list
podman network ls

# Remove a network
podman network rm my-network







Important Options for Container Creation and Running 

-d, --detach: Run the container in the background. 

-i, --interactive: Keep STDIN open even if not attached. 

-t, --tty: Allocate a pseudo-TTY. 

-p, --publish: Publish a container's port to the host. 

-v, --volume: Bind mount a volume. -e, 

--env: Set environment variables. 

--name: Assign a name to the container.




Interactive mode: 

podman run -it ubuntu:latest /bin/bash


Detached mode: 

podman run -d nginx:latest


With port mapping: 

podman run -d -p 8080:80 nginx:latest



The left side is yours — the host machine.
The right side is the port inside the container.
So in:
-p 8080:8080
the first 8080 is your machine’s port, the second 8080 is the container’s port.
If you did:
-p 9000:8080
you would reach SearXNG at http://localhost:9000, even though the container still thinks it’s serving on port 8080 internally.



podman pull docker.io/searxng/searxng


podman run --rm -d -p 8080:8080 searxng/searxng


podman run --rm -d \
  --network my-network \
  -p 8080:8080 \
  searxng/searxng



podman info

podman system prune

podman stats

podman events

podman system df -v






podman info --format '{{.Store.GraphDriverName}}'

Create a new volume: 

podman volume create myvolume

podman volume ls

podman volume rm myvolume




